[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
numpy = ">=1.12.0"
opencv-python = ">=3.2.0.0"
pandas = ">=0.19.0"

[dev-packages]
hypothesis = ">=3.6.0"
pytest = ">=3.0.0"
setuptools = "*"
"e1839a8" = {path = ".", editable = true}

[requires]
python_version = ">=3.4"
